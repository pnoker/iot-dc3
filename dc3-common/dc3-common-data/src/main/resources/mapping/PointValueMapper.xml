<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ Copyright 2016-present the IoT DC3 original author or authors.
  ~
  ~ This program is free software: you can redistribute it and/or modify
  ~ it under the terms of the GNU Affero General Public License as
  ~ published by the Free Software Foundation, either version 3 of the
  ~ License, or (at your option) any later version.
  ~
  ~ This program is distributed in the hope that it will be useful,
  ~ but WITHOUT ANY WARRANTY; without even the implied warranty of
  ~ MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
  ~ GNU Affero General Public License for more details.
  ~
  ~ You should have received a copy of the GNU Affero General Public License
  ~ along with this program.  If not, see <https://www.gnu.org/licenses/>.
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.pnoker.common.data.mapper.PointValueMapper">

    <!--
        MySQL fork: ON DUPLICATE KEY UPDATE with the fencing tuple guard
        expanded into per-column IF chains — MySQL has neither row-value
        comparison nor a WHERE clause on ON DUPLICATE KEY UPDATE. Each SET
        keeps the old value unless the incoming envelope wins the
        (fencing_token, create_time, sequence, message_id) lexicographic race.
    -->
    <insert id="upsertLatestBatch" databaseId="mysql">
        INSERT INTO dc3_point_latest
        (tenant_id, device_id, point_id, message_id, schema_version, driver_node,
         sequence, fencing_token, raw_value, cal_value, num_value, driver_id, create_time, operate_time)
        VALUES
        <foreach collection="values" item="value" separator=",">
            (#{value.tenantId}, #{value.deviceId}, #{value.pointId}, #{value.messageId},
             #{value.schemaVersion}, #{value.driverNode}, #{value.sequence}, #{value.fencingToken},
             #{value.rawValue}, #{value.calValue}, #{value.numValue}, #{value.driverId},
             #{value.createTime}, #{value.operateTime})
        </foreach>
        ON DUPLICATE KEY UPDATE
            message_id = IF(
                VALUES(fencing_token) > fencing_token
                OR (VALUES(fencing_token) = fencing_token AND VALUES(create_time) > create_time)
                OR (VALUES(fencing_token) = fencing_token AND VALUES(create_time) = create_time
                    AND VALUES(sequence) > sequence)
                OR (VALUES(fencing_token) = fencing_token AND VALUES(create_time) = create_time
                    AND VALUES(sequence) = sequence AND VALUES(message_id) > message_id),
                VALUES(message_id), message_id),
            schema_version = IF(VALUES(message_id) = message_id, VALUES(schema_version), schema_version),
            driver_node = IF(VALUES(message_id) = message_id, VALUES(driver_node), driver_node),
            sequence = IF(VALUES(message_id) = message_id, VALUES(sequence), sequence),
            fencing_token = IF(VALUES(message_id) = message_id, VALUES(fencing_token), fencing_token),
            raw_value = IF(VALUES(message_id) = message_id, VALUES(raw_value), raw_value),
            cal_value = IF(VALUES(message_id) = message_id, VALUES(cal_value), cal_value),
            num_value = IF(VALUES(message_id) = message_id, VALUES(num_value), num_value),
            driver_id = IF(VALUES(message_id) = message_id, VALUES(driver_id), driver_id),
            create_time = IF(VALUES(message_id) = message_id, VALUES(create_time), create_time),
            operate_time = IF(VALUES(message_id) = message_id, VALUES(operate_time), operate_time)
    </insert>

    <insert id="upsertLatestBatch">
        INSERT INTO dc3_point_latest
        (tenant_id, device_id, point_id, message_id, schema_version, driver_node,
         sequence, fencing_token, raw_value, cal_value, num_value, driver_id, create_time, operate_time)
        VALUES
        <foreach collection="values" item="value" separator=",">
            (#{value.tenantId}, #{value.deviceId}, #{value.pointId}, #{value.messageId},
             #{value.schemaVersion}, #{value.driverNode}, #{value.sequence}, #{value.fencingToken},
             #{value.rawValue}, #{value.calValue}, #{value.numValue}, #{value.driverId},
             #{value.createTime}, #{value.operateTime})
        </foreach>
        ON CONFLICT (tenant_id, device_id, point_id) DO UPDATE SET
            message_id = EXCLUDED.message_id,
            schema_version = EXCLUDED.schema_version,
            driver_node = EXCLUDED.driver_node,
            sequence = EXCLUDED.sequence,
            fencing_token = EXCLUDED.fencing_token,
            raw_value = EXCLUDED.raw_value,
            cal_value = EXCLUDED.cal_value,
            num_value = EXCLUDED.num_value,
            driver_id = EXCLUDED.driver_id,
            create_time = EXCLUDED.create_time,
            operate_time = EXCLUDED.operate_time
        WHERE (EXCLUDED.fencing_token, EXCLUDED.create_time, EXCLUDED.sequence, EXCLUDED.message_id)
            &gt; (dc3_point_latest.fencing_token, dc3_point_latest.create_time,
                  dc3_point_latest.sequence, dc3_point_latest.message_id)
    </insert>

    <!-- Shared latest-value projection; safe across multiple Data Center replicas. -->
    <select id="selectLatestPointValues" resultType="io.github.pnoker.common.data.entity.model.PointValueDO">
        SELECT tenant_id, device_id, point_id, message_id, schema_version,
               driver_node, sequence, fencing_token, raw_value, cal_value, num_value,
               driver_id, create_time, operate_time
        FROM dc3_point_latest
        WHERE tenant_id = #{tenantId}
        AND device_id = #{deviceId}
        AND point_id IN
        <foreach collection="pointIds" item="pointId" open="(" separator="," close=")">
            #{pointId}
        </foreach>
    </select>

    <!--
        Dashboard live stream: the latest row per (device, point) series,
        newest first. Served from the projection — the design keeps every
        latest-value read off the time-series store (§9.4).
    -->
    <select id="selectLatestStream" resultType="io.github.pnoker.common.data.entity.model.PointValueDO">
        SELECT tenant_id, device_id, point_id, message_id, schema_version,
               driver_node, sequence, fencing_token, raw_value, cal_value, num_value,
               driver_id, create_time, operate_time
        FROM dc3_point_latest
        WHERE tenant_id = #{tenantId}
        ORDER BY create_time DESC
        LIMIT #{limit}
    </select>

</mapper>
