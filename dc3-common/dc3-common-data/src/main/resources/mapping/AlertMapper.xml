<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ Copyright 2016-present the IoT DC3 original author or authors.
  ~
  ~ This program is free software: you can redistribute it and/or modify
  ~ it under the terms of the GNU Affero General Public License as
  ~ published by the Free Software Foundation, either version 3 of the
  ~ License, or (at your option) any later version.
  ~
  ~ This program is distributed in the hope that it will be useful,
  ~ but WITHOUT ANY WARRANTY; without even the implied warranty of
  ~ MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
  ~ GNU Affero General Public License for more details.
  ~
  ~ You should have received a copy of the GNU Affero General Public License
  ~ along with this program.  If not, see <https://www.gnu.org/licenses/>.
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.pnoker.common.data.mapper.AlertMapper">

    <select id="countAll" resultType="map">
        SELECT (SELECT COUNT(*)
                FROM dc3_device_event
                WHERE deleted = 0
                  AND tenant_id = #{tenantId})
                   + (SELECT COUNT(*)
                      FROM dc3_driver_event
                      WHERE deleted = 0
                        AND tenant_id = #{tenantId}) AS total,
               (SELECT COUNT(*)
                FROM dc3_device_event
                WHERE deleted = 0
                  AND tenant_id = #{tenantId}
                  AND confirm_flag = 0)
                   + (SELECT COUNT(*)
                      FROM dc3_driver_event
                      WHERE deleted = 0
                        AND tenant_id = #{tenantId}
                        AND confirm_flag = 0)        AS unconfirmed
    </select>

    <select id="countBySource" resultType="map">
        SELECT 'device'                        AS source,
               (SELECT COUNT(*)
                FROM dc3_device_event
                WHERE deleted = 0
                  AND tenant_id = #{tenantId}) AS total,
               (SELECT COUNT(*)
                FROM dc3_device_event
                WHERE deleted = 0
                  AND tenant_id = #{tenantId}
                  AND confirm_flag = 0)        AS unconfirmed
        UNION ALL
        SELECT 'driver'                        AS source,
               (SELECT COUNT(*)
                FROM dc3_driver_event
                WHERE deleted = 0
                  AND tenant_id = #{tenantId}) AS total,
               (SELECT COUNT(*)
                FROM dc3_driver_event
                WHERE deleted = 0
                  AND tenant_id = #{tenantId}
                  AND confirm_flag = 0)        AS unconfirmed
    </select>

    <select id="countByType" resultType="map">
        SELECT event_type_flag::text AS key, COUNT(*) AS count
        FROM (
            SELECT event_type_flag FROM dc3_device_event
            WHERE deleted = 0 AND tenant_id = #{tenantId}
            UNION ALL
            SELECT event_type_flag FROM dc3_driver_event
            WHERE deleted = 0 AND tenant_id = #{tenantId}
            ) merged
        GROUP BY event_type_flag
        ORDER BY count
        DESC
    </select>

    <!--
        Each branch has its own ORDER BY / LIMIT and must be wrapped in
        parentheses to participate in UNION ALL (Postgres rule). Driver rows
        have no point_id, so we project literal 0 to line up with the
        device branch's schema.
    -->
    <select id="latest" resultType="map">
        SELECT *
        FROM ((SELECT id,
                      'device'                              AS source,
                      device_id                             AS source_id,
                      point_id,
                      event_type_flag,
                      confirm_flag,
                      create_time,
                      COALESCE(event_ext ->> 'content', '') AS message
               FROM dc3_device_event
               WHERE deleted = 0
                 AND tenant_id = #{tenantId}
               ORDER BY create_time DESC
               LIMIT #{limit})
              UNION ALL
              (SELECT id,
                      'driver'                              AS source,
                      driver_id                             AS source_id,
                      0,
                      event_type_flag,
                      confirm_flag,
                      create_time,
                      COALESCE(event_ext ->> 'content', '') AS message
               FROM dc3_driver_event
               WHERE deleted = 0
                 AND tenant_id = #{tenantId}
               ORDER BY create_time DESC
               LIMIT #{limit})) unioned
        ORDER BY create_time DESC
        LIMIT #{limit}
    </select>

    <!--
        Common projection for a single event row. Included from the paging /
        latest selects so both return the same shape (id, source, sourceId,
        pointId, event_type_flag, confirm_flag, create_time, message).
        message comes out of event_ext JSON via the "content" key; SQL reads
        (event_ext->>'content') which works for both the new JsonExt layout
        and a bare {"content": ...} wrapper.
    -->

    <!--
        Per-hour event count since {from}. date_trunc('hour') buckets events
        at hour boundaries; service layer then spreads rows into a fixed-
        length sparkline array anchored to "now - 24h".
    -->
    <select id="hourlyCounts" resultType="map">
        SELECT date_trunc('hour', create_time) AS bucket, COUNT(*) AS count
        FROM (SELECT create_time
              FROM dc3_device_event
              WHERE deleted = 0
                AND tenant_id = #{tenantId}
                AND create_time &gt;= #{from}
              UNION ALL
              SELECT create_time
              FROM dc3_driver_event
              WHERE deleted = 0
                AND tenant_id = #{tenantId}
                AND create_time &gt;= #{from}) merged
        GROUP BY bucket
        ORDER BY bucket ASC
    </select>

    <!--
        Paged event list. source='device' or 'driver' restricts to one
        table (faster); otherwise UNION ALL across both with consistent
        shape. Inner LIMIT keeps each branch bounded before UNION.
    -->
    <select id="listPaged" resultType="map">
        SELECT * FROM (
        <choose>
            <when test="source == 'device'">
                SELECT id, 'device' AS source, device_id AS source_id, point_id,
                event_type_flag, confirm_flag, create_time,
                COALESCE(event_ext->>'content', '') AS message
                FROM dc3_device_event
                WHERE deleted = 0 AND tenant_id = #{tenantId}
                <if test="eventTypeFlag != null">AND event_type_flag = #{eventTypeFlag}</if>
                <if test="confirmFlag != null">AND confirm_flag = #{confirmFlag}</if>
            </when>
            <when test="source == 'driver'">
                SELECT id, 'driver' AS source, driver_id AS source_id, 0 AS point_id,
                event_type_flag, confirm_flag, create_time,
                COALESCE(event_ext->>'content', '') AS message
                FROM dc3_driver_event
                WHERE deleted = 0 AND tenant_id = #{tenantId}
                <if test="eventTypeFlag != null">AND event_type_flag = #{eventTypeFlag}</if>
                <if test="confirmFlag != null">AND confirm_flag = #{confirmFlag}</if>
            </when>
            <otherwise>
                (SELECT id, 'device' AS source, device_id AS source_id, point_id,
                event_type_flag, confirm_flag, create_time,
                COALESCE(event_ext->>'content', '') AS message
                FROM dc3_device_event
                WHERE deleted = 0 AND tenant_id = #{tenantId}
                <if test="eventTypeFlag != null">AND event_type_flag = #{eventTypeFlag}</if>
                <if test="confirmFlag != null">AND confirm_flag = #{confirmFlag}</if>)
                UNION ALL
                (SELECT id, 'driver' AS source, driver_id AS source_id, 0 AS point_id,
                event_type_flag, confirm_flag, create_time,
                COALESCE(event_ext->>'content', '') AS message
                FROM dc3_driver_event
                WHERE deleted = 0 AND tenant_id = #{tenantId}
                <if test="eventTypeFlag != null">AND event_type_flag = #{eventTypeFlag}</if>
                <if test="confirmFlag != null">AND confirm_flag = #{confirmFlag}</if>)
            </otherwise>
        </choose>
        ) rows
        ORDER BY create_time DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countFiltered" resultType="long">
        <choose>
            <when test="source == 'device'">
                SELECT COUNT(*) FROM dc3_device_event
                WHERE deleted = 0 AND tenant_id = #{tenantId}
                <if test="eventTypeFlag != null">AND event_type_flag = #{eventTypeFlag}</if>
                <if test="confirmFlag != null">AND confirm_flag = #{confirmFlag}</if>
            </when>
            <when test="source == 'driver'">
                SELECT COUNT(*) FROM dc3_driver_event
                WHERE deleted = 0 AND tenant_id = #{tenantId}
                <if test="eventTypeFlag != null">AND event_type_flag = #{eventTypeFlag}</if>
                <if test="confirmFlag != null">AND confirm_flag = #{confirmFlag}</if>
            </when>
            <otherwise>
                SELECT
                (SELECT COUNT(*) FROM dc3_device_event
                WHERE deleted = 0 AND tenant_id = #{tenantId}
                <if test="eventTypeFlag != null">AND event_type_flag = #{eventTypeFlag}</if>
                <if test="confirmFlag != null">AND confirm_flag = #{confirmFlag}</if>)
                + (SELECT COUNT(*) FROM dc3_driver_event
                WHERE deleted = 0 AND tenant_id = #{tenantId}
                <if test="eventTypeFlag != null">AND event_type_flag = #{eventTypeFlag}</if>
                <if test="confirmFlag != null">AND confirm_flag = #{confirmFlag}</if>)
            </otherwise>
        </choose>
    </select>

    <update id="confirmOne">
        <choose>
            <when test="source == 'device'">
                UPDATE dc3_device_event SET confirm_flag = 1
                WHERE id = #{id} AND tenant_id = #{tenantId} AND deleted = 0
            </when>
            <when test="source == 'driver'">
                UPDATE dc3_driver_event SET confirm_flag = 1
                WHERE id = #{id} AND tenant_id = #{tenantId} AND deleted = 0
            </when>
        </choose>
    </update>

    <update id="unconfirmOne">
        <choose>
            <when test="source == 'device'">
                UPDATE dc3_device_event SET confirm_flag = 0
                WHERE id = #{id} AND tenant_id = #{tenantId} AND deleted = 0
            </when>
            <when test="source == 'driver'">
                UPDATE dc3_driver_event SET confirm_flag = 0
                WHERE id = #{id} AND tenant_id = #{tenantId} AND deleted = 0
            </when>
        </choose>
    </update>

</mapper>
