<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ Copyright 2016-present the IoT DC3 original author or authors.
  ~
  ~ This program is free software: you can redistribute it and/or modify
  ~ it under the terms of the GNU Affero General Public License as
  ~ published by the Free Software Foundation, either version 3 of the
  ~ License, or (at your option) any later version.
  ~
  ~ This program is distributed in the hope that it will be useful,
  ~ but WITHOUT ANY WARRANTY; without even the implied warranty of
  ~ MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
  ~ GNU Affero General Public License for more details.
  ~
  ~ You should have received a copy of the GNU Affero General Public License
  ~ along with this program.  If not, see <https://www.gnu.org/licenses/>.
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.pnoker.common.manager.mapper.DashboardMapper">

    <select id="countDriverByEnable" resultType="map">
        SELECT enable_flag AS enable_flag, COUNT(*) AS count
        FROM dc3_driver
        WHERE deleted = 0
          AND tenant_id = #{tenantId}
        GROUP BY enable_flag
    </select>

    <select id="countDriverByType" resultType="map">
        SELECT driver_type_flag AS driver_type_flag, COUNT(*) AS count
        FROM dc3_driver
        WHERE deleted = 0
          AND tenant_id = #{tenantId}
        GROUP BY driver_type_flag
    </select>

    <!--
        driver_type_flag is only client/server — the real "protocol" identity
        lives in service_name (e.g. dc3-driver-modbus-tcp / dc3-driver-mqtt).
        Group by that so the home dashboard can show the actual protocol mix.
    -->
    <select id="countDriverByService" resultType="map">
        SELECT service_name AS service_name, COUNT(*) AS count
        FROM dc3_driver
        WHERE deleted = 0
          AND tenant_id = #{tenantId}
        GROUP BY service_name
        ORDER BY count DESC
    </select>

    <select id="countDeviceByEnable" resultType="map">
        SELECT enable_flag AS enable_flag, COUNT(*) AS count
        FROM dc3_device
        WHERE deleted = 0
          AND tenant_id = #{tenantId}
        GROUP BY enable_flag
    </select>

    <select id="countDeviceByDriver" resultType="map">
        SELECT driver_id AS driver_id, COUNT(*) AS count
        FROM dc3_device
        WHERE deleted = 0
          AND tenant_id = #{tenantId}
        GROUP BY driver_id
        ORDER BY count DESC
        LIMIT #{limit}
    </select>

    <!--
        One device can bind to multiple profiles, so the total here can
        exceed the device count. Only active bindings participate.
    -->
    <select id="countDeviceByProfile" resultType="map">
        SELECT b.profile_id AS profile_id, COUNT(*) AS count
        FROM dc3_profile_bind b
        WHERE b.deleted = 0
          AND b.tenant_id = #{tenantId}
        GROUP BY b.profile_id
        ORDER BY count DESC
        LIMIT #{limit}
    </select>

    <!--
        Daily new-row count over the given window. ${table} is inlined
        (service layer whitelists to dc3_driver / dc3_device / dc3_point).
        Missing days are filled with zero on the service side so the
        frontend sparkline always gets a fixed-length array.
    -->
    <select id="dailyGrowth" resultType="map">
        SELECT DATE(create_time) AS day, COUNT(*) AS count
        FROM ${table}
        WHERE deleted = 0
          AND tenant_id = #{tenantId}
          AND create_time &gt;= #{from}
          AND create_time &lt; #{to}
        GROUP BY DATE(create_time)
        ORDER BY day ASC
    </select>

</mapper>
