apiVersion: v1
kind: Service
metadata:
  name: dc3-agentic
  namespace: dc3
  labels:
    app.kubernetes.io/name: dc3-agentic
    app.kubernetes.io/part-of: iot-dc3
spec:
  type: ClusterIP
  selector:
    app: dc3-agentic
  ports:
    - name: http
      port: 8600
      targetPort: http
