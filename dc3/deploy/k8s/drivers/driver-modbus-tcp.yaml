apiVersion: apps/v1
kind: Deployment
metadata:
  name: dc3-driver-modbus-tcp
  namespace: dc3
  labels:
    app.kubernetes.io/name: dc3-driver-modbus-tcp
    app.kubernetes.io/part-of: iot-dc3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dc3-driver-modbus-tcp
  template:
    metadata:
      labels:
        app: dc3-driver-modbus-tcp
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: dc3-driver-modbus-tcp
          image: pnoker/dc3-driver-modbus-tcp:2026.6
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: dc3-config
            - secretRef:
                name: dc3-secrets
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: "0.5"
              memory: 512Mi
          volumeMounts:
            - name: logs
              mountPath: /dc3-driver/dc3-driver-modbus-tcp/dc3/logs
            - name: data
              mountPath: /dc3-driver/dc3-driver-modbus-tcp/dc3/data
      volumes:
        - name: logs
          emptyDir: { }
        - name: data
          emptyDir: { }
