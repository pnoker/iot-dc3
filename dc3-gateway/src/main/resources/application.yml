server:
  port: 8000

spring:
  application:
    name: @project.artifactId@
  main:
    allow-bean-definition-overriding: true
  thymeleaf:
    cache: false
    enabled: true
    check-template: true
    mode: HTML5
    encoding: UTF-8
    prefix: classpath:/templates/
    suffix: .html
    servlet:
      content-type: text/html
  security:
    encode:
      key: t8KCtK0y63kA3kUx
    user:
      name: iotdc3
      password: iotdc3
  redis:
    host: iotdc3.redis
    port: 6379
    password: ${spring.security.user.password}
    database: 2
    timeout: 1H
  cloud:
    gateway:
      loadbalancer:
        use404: true
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        # 权限中心
        - id: auth_rest_auth
          uri: lb://dc3-auth
          predicates:
            - Path=/api/v3/auth/**
          filters:
            - PasswordDecoderFilter
            - name: RequestRateLimiter
              args:
                key-resolver: '#{@ipKeyResolver}'
                redis-rate-limiter.replenishRate: 10
                redis-rate-limiter.burstCapacity: 20
            - name: Hystrix
              args:
                name: default
                fallbackUri: 'forward:/fallback'
        # 设备管理
        - id: device_rest_device_manager
          uri: lb://dc3-manager
          predicates:
            - Path=/api/v3/device/**
          filters:
            - name: RequestRateLimiter
              args:
                key-resolver: '#{@ipKeyResolver}'
                redis-rate-limiter.replenishRate: 10
                redis-rate-limiter.burstCapacity: 20
            - name: Hystrix
              args:
                name: default
                fallbackUri: 'forward:/fallback'
        # 数据采集
        - id: data_rest_device_collect
          uri: lb://dc3-collect
          predicates:
            - Path=/api/v3/data/**
          filters:
            - name: RequestRateLimiter
              args:
                key-resolver: '#{@ipKeyResolver}'
                redis-rate-limiter.replenishRate: 10
                redis-rate-limiter.burstCapacity: 20
            - name: Hystrix
              args:
                name: default
                fallbackUri: 'forward:/fallback'
        # rtmp
        - id: rtmp_rest_rtmp
          uri: lb://dc3-rtmp
          predicates:
            - Path=/api/v3/rtmp/**
          filters:
            - name: RequestRateLimiter
              args:
                key-resolver: '#{@ipKeyResolver}'
                redis-rate-limiter.replenishRate: 10
                redis-rate-limiter.burstCapacity: 20
            - name: Hystrix
              args:
                name: default
                fallbackUri: 'forward:/fallback'
        # opc
        - id: opc_rest_opc
          uri: lb://dc3-opc
          predicates:
            - Path=/api/v3/opc/**
          filters:
            - name: RequestRateLimiter
              args:
                key-resolver: '#{@ipKeyResolver}'
                redis-rate-limiter.replenishRate: 10
                redis-rate-limiter.burstCapacity: 20
            - name: Hystrix
              args:
                name: default
                fallbackUri: 'forward:/fallback'
        # opc ua
        - id: opcua_rest_opc_ua
          uri: lb://dc3-opc-ua
          predicates:
            - Path=/api/v3/opcua/**
          filters:
            - name: RequestRateLimiter
              args:
                key-resolver: '#{@ipKeyResolver}'
                redis-rate-limiter.replenishRate: 10
                redis-rate-limiter.burstCapacity: 20
            - name: Hystrix
              args:
                name: default
                fallbackUri: 'forward:/fallback'
        # resource
        - id: resource_rest_resource
          uri: lb://dc3-resource
          predicates:
            - Path=/api/v3/resource/**
          filters:
            - name: RequestRateLimiter
              args:
                key-resolver: '#{@ipKeyResolver}'
                redis-rate-limiter.replenishRate: 10
                redis-rate-limiter.burstCapacity: 20
            - name: Hystrix
              args:
                name: default
                fallbackUri: 'forward:/fallback'

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: ALWAYS

logging:
  level:
    org.springframework.data: ERROR
    org.springframework.web: ERROR
    org.mybatis.spring: ERROR
    org.mongodb.driver: ERROR
    com.zaxxer.hikari: ERROR
    com.ulisesbocchio: ERROR
    io.lettuce.core: ERROR
    io.undertow: ERROR
    com.netflix: ERROR
  file:
    name: dc3/logs/${spring.application.name}.log

eureka:
  instance:
    prefer-ip-address: true
    health-check-url-path: /actuator/health
  client:
    service-url:
      defaultZone: http://${spring.security.user.name}:${spring.security.user.password}@iotdc3.register:8100/eureka/